#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    double A, B;
    cin >> A >> B;

    cout.precision(10);
    cout << A / B << "\n";
    return 0;
}